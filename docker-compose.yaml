version: "3.8"
services:
  api:
    build:
      context: ./back
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    volumes:
      - ./back:/app
    environment:
      - NODE_ENV=development
